export function isNumber(value) {
    return typeof value === 'number';
}
export function isString(value) {
    return typeof value === 'string';
}
export function isDate(value) {
    return value instanceof Date;
}
export function isValueFunc(value) {
    return typeof value === 'function';
}
export function isPlural(value) {
    return value !== null && typeof value === 'object';
}
export function isEmpty(value) {
    return Object.keys(value).every((prop) => !value[prop]);
}
export function getPreset(presets, name) {
    return isString(name) ? presets === null || presets === void 0 ? void 0 : presets[name] : presets === null || presets === void 0 ? void 0 : presets.default;
}
export function defineMessages(messages) {
    return messages;
}
